{
  "name": "squad-techstore-service",
  "version": "1.0.0",
  "description": "REST API service connecting to PostgreSQL for relational data storage and CRUD Operations",
  "main": "src/app.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "ts-node-dev --respawn --transpile-only src/app.ts"
  },
  "author": "David Gardner",
  "license": "MIT",
  "devDependencies": {
    "@types/express": "^4.17.21",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
